@echo off
echo PhotoPicker is a library, not a service.
echo Use it from another project:
echo     from photopicker import pick_photos
echo.
echo To run the CLI ^(if available^):
echo     .venv\Scripts\python.exe -m photopicker --help
echo.
pause
